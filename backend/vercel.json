{
    "version": 2,
    "builds": [
        {
            "src": "server.ts",
            "use": "vercel/node"
        },
        {
            "src": "src/**/**",
            "use": "@vercel/static" 
        }
    ],
    "routes": [
        { "src": "/(.*)", "dest": "server.ts",
          "headers": {
            "Access-Control-Allow-Origin": "*"
          }
        }
    ]
}